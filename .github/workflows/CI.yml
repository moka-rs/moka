name: CI

on:
  push:
    paths-ignore:
    - '.devcontainer/**'
    - '.vscode/**'
  pull_request:
    paths-ignore:
    - '.devcontainer/**'
    - '.vscode/**'
  schedule:
    # Run against the last commit on the default branch on Friday at 8pm (UTC?)
    - cron:  '0 20 * * 5'

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        rust:
          - stable
          - beta
          - 1.45.2  # MSRV

    steps:
      - name: Checkout Moka
        uses: actions/checkout@v2

      - name: Install Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: ${{ matrix.rust }}
          override: true
          components: rustfmt, clippy

      - name: Build (no features)
        uses: actions-rs/cargo@v1
        with:
          command: build

      - name: Run tests (no features)
        uses: actions-rs/cargo@v1
        with:
          command: test
          args: --release

      - name: Run tests (runtime-actix)
        uses: actions-rs/cargo@v1
        with:
          command: test
          args: --release --features runtime-actix

      - name: Run tests (runtime-async-std)
        uses: actions-rs/cargo@v1
        with:
          command: test
          args: --release --features runtime-async-std

      - name: Run tests (runtime-tokio)
        uses: actions-rs/cargo@v1
        with:
          command: test
          args: --release --features runtime-tokio

      - name: Run Rustfmt
        uses: actions-rs/cargo@v1
        with:
          command: fmt
          args: --all -- --check

      - name: Run Clippy
        uses: actions-rs/clippy-check@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          args: --features runtime-tokio -- -D warnings
